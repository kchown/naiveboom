extends layout
block content
	hr
	#app
		<div v-if='status==1'>
		pre {{ text }}
		</div>
		<div v-else-if='status==0'>
		h3 啊喔😯，你查看的内容已经不见了...
		p 这是因为这个网址已经被看过一次了
		| 被看过一次的内容就会自动消失
		| 对，这就是人们常说的 <b>阅后即焚！</b>
		a(href="/") 我也要生成一个！
		</div>
		<div v-else>
		p#warning 正在获取...
		</div>
	script(src='/javascripts/view.js')